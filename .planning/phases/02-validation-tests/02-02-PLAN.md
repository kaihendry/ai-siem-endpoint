---
id: "02-02"
phase: 2
wave: 2
depends_on:
  - "02-01"
name: "Unit and Handler Tests"
files_modified:
  - main_test.go
autonomous: true
requirements:
  - TEST-01
  - TEST-02
---

<objective>
Create main_test.go with unit tests for putEvent DynamoDB attribute construction and handler tests for POST / using net/http/httptest. This plan covers all four test scenarios: putEvent round-trip serialization, POST happy path, POST with missing required fields (400), and POST with oversized body (413).
</objective>

<must_haves>
  <truths>
    - go test ./... exits 0 with at least 4 tests passing
    - TestPutEventAttributes verifies DynamoDB attribute map keys and types
    - TestHandlePostHappyPath verifies HTTP 201 and JSON run_id in response
    - TestHandlePostMissingFields verifies HTTP 400 for missing run_id, timestamp, host
    - TestHandlePostBodyTooLarge verifies HTTP 413 for body > 1 MB
  </truths>
</must_haves>

<tasks>

<task id="T01">
  <name>Create main_test.go with putEvent unit test</name>
  <read_first>
    - main.go (putEvent function at line 133, AuditRun struct at line 42, Finding struct at line 32)
    - go.mod (module name)
  </read_first>
  <action>
    Create main_test.go in package main. Add TestPutEventAttributes that:
    1. Constructs an AuditRun with known values: RunID="test-run-1", Timestamp=time.Date(2024,1,15,10,0,0,0,time.UTC), Host="testhost", Score=90, ExitCode=0, DurationMs=500, one Finding{Type:"policy",Severity:"high",Module:"guardrails"}
    2. Calls putEvent using a fake DynamoDB client — use an interface or mock that captures the PutItemInput without making a real AWS call. Use a table-driven struct that implements the minimal PutItem signature.
    3. Asserts the captured item map contains: pk="all", sk starts with "2024-01-15T10:00:00Z#test-run-1", run_id="test-run-1", host="testhost", score numeric attribute = "90", findings is a non-empty JSON string containing "guardrails".

    Since putEvent uses the package-level dynamoClient, either refactor putEvent to accept a client interface or use a test-only setter. The simplest approach: extract a putItemFunc type or accept a minimal interface. Alternatively, define a testable wrapper. Follow whatever pattern minimizes changes to main.go — prefer a small interface extraction over a large refactor.
  </action>
  <acceptance_criteria>
    - main_test.go exists in package main
    - TestPutEventAttributes function is defined
    - go test -run TestPutEventAttributes ./... exits 0
  </acceptance_criteria>
</task>

<task id="T02">
  <name>Add httptest-based handler tests for POST /</name>
  <read_first>
    - main.go (handlePost at line 112, init function at line 22)
    - main_test.go (created in T01 — add to it, do not create a new file)
  </read_first>
  <action>
    Add to main_test.go three table-driven subtests under TestHandlePost that use net/http/httptest:

    1. Happy path: POST body = valid AuditRun JSON with run_id="happy-run", timestamp=RFC3339 string, host="host1". Stub or replace putEvent so it does not call DynamoDB (set tableName="" or use a test flag to short-circuit storage). Assert response status 201 and body contains "run_id".

    2. Missing required fields: three sub-cases — body missing run_id (empty string), body with zero timestamp (omit timestamp field), body with empty host. Each must return status 400 and body containing "error".

    3. Body too large: create a body of 1<<20 + 1 bytes. Assert response status 413.

    For test isolation of DynamoDB, the simplest approach is to initialize dynamoClient to nil in TestMain or use a build-tag-free approach: check if dynamoClient is nil in putEvent and return a sentinel error when called from tests. Or mock at the httptest level by patching handlePost to use a test double. Choose the least invasive approach that doesn't break the production code path.
  </action>
  <acceptance_criteria>
    - TestHandlePost function is defined in main_test.go
    - go test -run TestHandlePost ./... exits 0
    - go test ./... exits 0 (all tests pass)
  </acceptance_criteria>
</task>

</tasks>

<verification>
- go test ./... exits 0
- go test -v ./... output lists TestPutEventAttributes and TestHandlePost
- No new imports in main.go beyond what was added in 02-01
</verification>
