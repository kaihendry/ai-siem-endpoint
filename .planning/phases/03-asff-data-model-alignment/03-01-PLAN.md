---
phase: 03-asff-data-model-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - main.go
autonomous: true
requirements:
  - ASFF-01
  - ASFF-02

must_haves:
  truths:
    - "SeverityASFF and ResourceASFF structs exist in main.go"
    - "Finding struct has 7 new ASFF fields, all with omitempty, all with distinct JSON tags"
    - "AuditRun struct has 2 new run-level ASFF identity fields, both with omitempty"
    - "go build succeeds with no new imports required"
    - "Existing Finding and AuditRun fields are unchanged (no JSON tag renames)"
  artifacts:
    - path: "main.go"
      provides: "SeverityASFF, ResourceASFF types; extended Finding and AuditRun structs"
      contains: "SeverityASFF"
  key_links:
    - from: "Finding.ASFFSeverity"
      to: "SeverityASFF"
      via: "*SeverityASFF pointer field"
      pattern: "ASFFSeverity.*SeverityASFF"
    - from: "Finding.Resources"
      to: "ResourceASFF"
      via: "[]ResourceASFF slice field"
      pattern: "Resources.*\\[\\]ResourceASFF"
---

<objective>
Add ASFF-required struct types and extend the existing Finding and AuditRun structs with new optional fields. All new fields use omitempty so existing clients posting payloads without ASFF fields continue to work unchanged.

Purpose: Establish the Go type system that the rest of Phase 3 builds on — storage changes (Plan 02) and tests (Plan 03) both depend on these types being in place.
Output: Two new nested structs (SeverityASFF, ResourceASFF) and extended Finding + AuditRun in main.go, with go build passing.
</objective>

<execution_context>
@/home/hendry/ai-siem-endpoint/.claude/get-shit-done/workflows/execute-plan.md
@/home/hendry/ai-siem-endpoint/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/hendry/ai-siem-endpoint/.planning/ROADMAP.md
@/home/hendry/ai-siem-endpoint/.planning/phases/03-asff-data-model-alignment/03-RESEARCH.md

<interfaces>
<!-- Current structs in main.go that this plan modifies. -->

Current Finding struct (main.go:34-42):
```go
type Finding struct {
    Type        string   `json:"type"`
    Severity    string   `json:"severity"`
    Module      string   `json:"module"`
    Resource    string   `json:"resource"`
    Description string   `json:"description"`
    Remediation string   `json:"remediation"`
    Confidence  *float64 `json:"confidence,omitempty"`
}
```

Current AuditRun struct (main.go:44-56):
```go
type AuditRun struct {
    SchemaVersion string    `json:"schema_version"`
    RunID         string    `json:"run_id"`
    Timestamp     time.Time `json:"timestamp"`
    Host          string    `json:"host"`
    User          string    `json:"user"`
    Mode          string    `json:"mode"`
    Version       string    `json:"version"`
    Findings      []Finding `json:"findings"`
    Score         int       `json:"score"`
    ExitCode      int       `json:"exit_code"`
    DurationMs    int64     `json:"duration_ms"`
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add SeverityASFF and ResourceASFF nested types</name>
  <files>main.go</files>
  <read_first>
    - main.go (full file — understand current struct layout and insertion point after line 42)
    - .planning/phases/03-asff-data-model-alignment/03-RESEARCH.md (struct definitions section)
  </read_first>
  <behavior>
    - SeverityASFF has fields: Label string (json:"Label,omitempty"), Original string (json:"Original,omitempty")
    - ResourceASFF has fields: ID string (json:"Id"), Type string (json:"Type"), Region string (json:"Region,omitempty"), Partition string (json:"Partition,omitempty")
    - Both types are inserted after the existing Finding struct definition (after line 42 of current main.go)
    - go build ./... succeeds after insertion
    - go vet ./... reports no issues
  </behavior>
  <action>
Insert the two new nested struct types into main.go immediately after the closing brace of the Finding struct (currently ending around line 42). Place them before the AuditRun struct.

SeverityASFF struct:
- Field Label: type string, json tag "Label,omitempty"
- Field Original: type string, json tag "Original,omitempty"
- (Do NOT add Normalized *int — per research, Normalized is deprecated; Label-only is correct)

ResourceASFF struct:
- Field ID: type string, json tag "Id" (required in ASFF — no omitempty)
- Field Type: type string, json tag "Type" (required in ASFF — no omitempty)
- Field Region: type string, json tag "Region,omitempty"
- Field Partition: type string, json tag "Partition,omitempty"

No new imports are needed. Do not alter any existing struct, function, or import.
  </action>
  <verify>
    <automated>cd /home/hendry/ai-siem-endpoint && go build ./... && go vet ./...</automated>
  </verify>
  <done>main.go compiles; SeverityASFF and ResourceASFF types are present with the exact fields and JSON tags specified above; no existing struct is modified.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend Finding and AuditRun with ASFF fields</name>
  <files>main.go</files>
  <read_first>
    - main.go (re-read after Task 1 completes to see current state with new nested types)
  </read_first>
  <behavior>
    - Finding gains 7 new fields after Confidence, all omitempty, with exact JSON tags listed below
    - AuditRun gains 2 new fields after DurationMs, both omitempty
    - All 7 existing Finding fields retain their original JSON tags unchanged
    - All 11 existing AuditRun fields retain their original JSON tags unchanged
    - json.Unmarshal of an old payload (no ASFF keys) into AuditRun/Finding succeeds with zero values for new fields
    - json.Marshal of an AuditRun/Finding with all new fields set produces correct JSON keys
    - go build ./... and go vet ./... pass
  </behavior>
  <action>
Extend the Finding struct by appending these 7 fields after the existing Confidence field. Separate them from existing fields with a blank line and a comment "// ASFF fields — all omitempty for backwards compatibility":

  ID           string         `json:"Id,omitempty"`
  Title        string         `json:"title,omitempty"`
  GeneratorId  string         `json:"generator_id,omitempty"`
  ASFFTypes    []string       `json:"asff_types,omitempty"`
  ASFFSeverity *SeverityASFF  `json:"asff_severity,omitempty"`
  Resources    []ResourceASFF `json:"resources,omitempty"`
  CreatedAt    string         `json:"created_at,omitempty"`
  UpdatedAt    string         `json:"updated_at,omitempty"`

Note on json tags:
- ID uses "Id" (capital I, lowercase d) — matches ASFF wire format
- title, generator_id, asff_types, asff_severity, created_at, updated_at use lowercase/snake_case to match the existing tag convention in the rest of Finding
- ASFFSeverity uses tag "asff_severity" (not "Severity") to avoid collision with the existing Severity string field which keeps tag "severity"

Extend the AuditRun struct by appending these 2 fields after DurationMs, with a blank line and comment "// ASFF run-level identity fields — omitempty for backwards compatibility":

  ProductArn   string `json:"product_arn,omitempty"`
  AwsAccountId string `json:"aws_account_id,omitempty"`

Do not rename, remove, or reorder any existing struct field.
  </action>
  <verify>
    <automated>cd /home/hendry/ai-siem-endpoint && go build ./... && go vet ./... && go test ./... -run TestHandlePost</automated>
  </verify>
  <done>go build, go vet, and existing TestHandlePost tests all pass. Finding struct has 15 fields total (7 existing + 8 new including Confidence). AuditRun struct has 13 fields total (11 existing + 2 new). All new fields have omitempty.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client→POST / | Untrusted JSON payload decoded into AuditRun; new ASFF string fields are user-controlled |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Tampering | Finding.ID, Finding.Title, AuditRun.ProductArn | accept | Stored verbatim as strings in DynamoDB String attribute; no execution or routing based on value; html/template auto-escapes on output |
| T-03-02 | Denial of Service | Finding.Resources []ResourceASFF | accept | Existing 1 MiB MaxBytesReader in handlePost limits total payload size; no additional per-field size check required |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase — no package install tasks |
</threat_model>

<verification>
After both tasks:
- `go build ./...` exits 0
- `go vet ./...` exits 0
- `go test ./...` exits 0 (all existing tests still pass)
- `grep -c "SeverityASFF" /home/hendry/ai-siem-endpoint/main.go` returns >= 2 (type definition + reference in Finding)
- `grep -c "ResourceASFF" /home/hendry/ai-siem-endpoint/main.go` returns >= 2 (type definition + reference in Finding)
- `grep "ProductArn" /home/hendry/ai-siem-endpoint/main.go` shows the AuditRun field with tag `json:"product_arn,omitempty"`
- `grep "AwsAccountId" /home/hendry/ai-siem-endpoint/main.go` shows the AuditRun field with tag `json:"aws_account_id,omitempty"`
</verification>

<success_criteria>
1. SeverityASFF and ResourceASFF types defined in main.go with correct fields and JSON tags
2. Finding struct has 8 new fields (ID, Title, GeneratorId, ASFFTypes, ASFFSeverity, Resources, CreatedAt, UpdatedAt), all omitempty
3. AuditRun struct has 2 new fields (ProductArn, AwsAccountId), both omitempty
4. `go build ./...` and `go test ./...` pass with zero changes to existing test cases
5. No existing JSON tag on Finding or AuditRun is modified
</success_criteria>

<output>
Create `.planning/phases/03-asff-data-model-alignment/03-01-SUMMARY.md` when done
</output>
