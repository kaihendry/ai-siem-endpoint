---
phase: 03-asff-data-model-alignment
plan: 03
type: execute
wave: 3
depends_on:
  - 03-01
  - 03-02
files_modified:
  - main_test.go
autonomous: true
requirements:
  - ASFF-05

must_haves:
  truths:
    - "TestPutEventAttributes asserts that product_arn and aws_account_id attributes are present with correct values"
    - "TestPutEventAttributes asserts that the findings JSON blob contains the new ASFF Finding fields (ID, asff_severity.Label)"
    - "TestHandlePost has a new sub-test that posts an ASFF-extended payload and returns 201"
    - "go test ./... passes with all tests green"
    - "Old (non-ASFF) payload test still returns 201 — backwards compat verified by existing happy-path test"
  artifacts:
    - path: "main_test.go"
      provides: "ASFF field assertions in TestPutEventAttributes; ASFF round-trip sub-test in TestHandlePost"
      contains: "product_arn"
  key_links:
    - from: "TestPutEventAttributes ASFF assertions"
      to: "putEvent item map"
      via: "mock.lastInput.Item[\"product_arn\"]"
      pattern: "product_arn.*AttributeValueMemberS"
    - from: "TestHandlePost ASFF sub-test"
      to: "handlePost"
      via: "httptest.NewRequest with ASFF payload"
      pattern: "StatusCreated"
---

<objective>
Extend the existing test suite to cover the new ASFF fields: add assertions to TestPutEventAttributes for the two new DynamoDB attributes and the ASFF Finding fields in the blob, and add one new sub-test to TestHandlePost verifying a full ASFF-extended payload round-trips through the handler.

Purpose: Satisfies ASFF-05 (tests cover new ASFF fields). Depends on Plans 03-01 and 03-02 having completed the struct and storage changes.
Output: main_test.go with extended TestPutEventAttributes and a new TestHandlePost/asff_payload sub-test; `go test ./...` green.
</objective>

<execution_context>
@/home/hendry/ai-siem-endpoint/.claude/get-shit-done/workflows/execute-plan.md
@/home/hendry/ai-siem-endpoint/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/home/hendry/ai-siem-endpoint/.planning/ROADMAP.md
@/home/hendry/ai-siem-endpoint/.planning/phases/03-asff-data-model-alignment/03-RESEARCH.md
@/home/hendry/ai-siem-endpoint/.planning/phases/03-asff-data-model-alignment/03-01-SUMMARY.md
@/home/hendry/ai-siem-endpoint/.planning/phases/03-asff-data-model-alignment/03-02-SUMMARY.md

<interfaces>
<!-- Existing test infrastructure in main_test.go to build on. -->

mockPutter (main_test.go:18-26):
```go
type mockPutter struct {
    lastInput *dynamodb.PutItemInput
    err       error
}
func (m *mockPutter) PutItem(_ context.Context, params *dynamodb.PutItemInput, _ ...func(*dynamodb.Options)) (*dynamodb.PutItemOutput, error) {
    m.lastInput = params
    return &dynamodb.PutItemOutput{}, m.err
}
```

TestPutEventAttributes structure (main_test.go:37-105):
- Creates a mock, assigns to eventPutter
- Builds an AuditRun value and calls putEvent
- Asserts on mock.lastInput.Item entries using type assertion to *types.AttributeValueMemberS or *types.AttributeValueMemberN

TestHandlePost sub-test pattern (main_test.go:109-202):
- Uses postJSON helper (builds httptest.Request, calls handlePost, returns *httptest.ResponseRecorder)
- Sub-tests: t.Run("name", func(t *testing.T) { ... })
- Checks rr.Code and rr.Body.String()

AuditRun new fields (from Plan 03-01):
- ProductArn   string `json:"product_arn,omitempty"`
- AwsAccountId string `json:"aws_account_id,omitempty"`

Finding new fields (from Plan 03-01):
- ID          string         `json:"Id,omitempty"`
- Title       string         `json:"title,omitempty"`
- GeneratorId string         `json:"generator_id,omitempty"`
- ASFFTypes   []string       `json:"asff_types,omitempty"`
- ASFFSeverity *SeverityASFF `json:"asff_severity,omitempty"`
- Resources   []ResourceASFF `json:"resources,omitempty"`
- CreatedAt   string         `json:"created_at,omitempty"`
- UpdatedAt   string         `json:"updated_at,omitempty"`

SeverityASFF (from Plan 03-01):
- Label    string `json:"Label,omitempty"`
- Original string `json:"Original,omitempty"`

ResourceASFF (from Plan 03-01):
- ID        string `json:"Id"`
- Type      string `json:"Type"`
- Region    string `json:"Region,omitempty"`
- Partition string `json:"Partition,omitempty"`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend TestPutEventAttributes with ASFF field assertions</name>
  <files>main_test.go</files>
  <read_first>
    - main_test.go (full file — understand the AuditRun and Finding values used in TestPutEventAttributes)
    - main.go (confirm exact field names for ProductArn, AwsAccountId, SeverityASFF, ResourceASFF as added by Plan 03-01/02)
    - .planning/phases/03-asff-data-model-alignment/03-01-SUMMARY.md
    - .planning/phases/03-asff-data-model-alignment/03-02-SUMMARY.md
  </read_first>
  <behavior>
    - The AuditRun value in TestPutEventAttributes gains ProductArn and AwsAccountId fields
    - The Finding in the AuditRun gains: ID "finding-001", Title "Test Finding", GeneratorId "ai-check-guardrails/policy", ASFFSeverity &SeverityASFF{Label: "HIGH", Original: "HIGH"}, Resources []ResourceASFF{{ID: "arn:aws:ec2::123456789012:instance/i-abc", Type: "AwsEc2Instance"}}, CreatedAt "2024-01-15T10:00:00Z", UpdatedAt "2024-01-15T10:00:00Z"
    - After the existing assertions, three new assertions are added:
      1. item["product_arn"] is *types.AttributeValueMemberS with Value "arn:aws:securityhub:eu-west-2:123456789012:product/123456789012/default"
      2. item["aws_account_id"] is *types.AttributeValueMemberS with Value "123456789012"
      3. The findings JSON blob (item["findings"].Value) contains both "finding-001" and "\"Label\":\"HIGH\""
    - go test -run TestPutEventAttributes passes
  </behavior>
  <action>
In TestPutEventAttributes, make two changes:

CHANGE 1 — Update the AuditRun literal to include ASFF fields:
Add to the AuditRun struct literal:
    ProductArn:   "arn:aws:securityhub:eu-west-2:123456789012:product/123456789012/default",
    AwsAccountId: "123456789012",

Update the single Finding in the Findings slice to include:
    ID:           "finding-001",
    Title:        "Test Finding",
    GeneratorId:  "ai-check-guardrails/policy",
    ASFFSeverity: &SeverityASFF{Label: "HIGH", Original: "HIGH"},
    Resources:    []ResourceASFF{{ID: "arn:aws:ec2::123456789012:instance/i-abc", Type: "AwsEc2Instance"}},
    CreatedAt:    "2024-01-15T10:00:00Z",
    UpdatedAt:    "2024-01-15T10:00:00Z",

CHANGE 2 — Add three assertions after the existing findings assertion block (after the strings.Contains check for "guardrails"):

Assert product_arn attribute:
    productArnAttr, ok := item["product_arn"].(*types.AttributeValueMemberS)
    if !ok || productArnAttr.Value != "arn:aws:securityhub:eu-west-2:123456789012:product/123456789012/default" {
        t.Errorf("product_arn = %v, want correct ARN", item["product_arn"])
    }

Assert aws_account_id attribute:
    awsAccountAttr, ok := item["aws_account_id"].(*types.AttributeValueMemberS)
    if !ok || awsAccountAttr.Value != "123456789012" {
        t.Errorf("aws_account_id = %v, want \"123456789012\"", item["aws_account_id"])
    }

Assert findings blob contains new ASFF Finding fields (reuse the findingsAttr variable already declared above):
    if !strings.Contains(findingsAttr.Value, "finding-001") {
        t.Errorf("findings JSON %q does not contain \"finding-001\"", findingsAttr.Value)
    }
    if !strings.Contains(findingsAttr.Value, `"Label":"HIGH"`) {
        t.Errorf("findings JSON %q does not contain ASFF severity label", findingsAttr.Value)
    }

Do not alter any other test or helper function.
  </action>
  <verify>
    <automated>cd /home/hendry/ai-siem-endpoint && go test ./... -run TestPutEventAttributes -v</automated>
  </verify>
  <done>TestPutEventAttributes passes with all original assertions plus the three new ASFF assertions. Output shows "PASS" with no failures.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add ASFF round-trip sub-test to TestHandlePost</name>
  <files>main_test.go</files>
  <read_first>
    - main_test.go (read after Task 1 to see current state; understand postJSON helper and sub-test structure)
  </read_first>
  <behavior>
    - A new sub-test "asff extended payload returns 201" is added inside TestHandlePost
    - The payload includes all ASFF extended fields: product_arn, aws_account_id, and a finding with Id, title, generator_id, asff_severity, resources, created_at, updated_at
    - The sub-test asserts rr.Code == 201
    - The sub-test asserts rr.Body.String() contains "run_id"
    - go test -run TestHandlePost passes with all 5 existing sub-tests plus the new one
  </behavior>
  <action>
Add one new sub-test inside TestHandlePost, after the existing "body too large returns 413" sub-test.

The sub-test name is "asff extended payload returns 201". It builds a payload map using map[string]any with these fields:

    "run_id":         "asff-run-1",
    "timestamp":      "2024-01-15T10:00:00Z",
    "host":           "ci-runner",
    "product_arn":    "arn:aws:securityhub:eu-west-2:123456789012:product/123456789012/default",
    "aws_account_id": "123456789012",
    "findings": []map[string]any{
        {
            "type":          "policy",
            "severity":      "HIGH",
            "module":        "guardrails",
            "description":   "Guardrail policy violation",
            "Id":            "arn:aws:securityhub:eu-west-2:123456789012:finding/abc-001",
            "title":         "Guardrail Policy Violation",
            "generator_id":  "ai-check-guardrails/policy",
            "asff_types":    []string{"Software and Configuration Checks/AWS Security Best Practices"},
            "asff_severity": map[string]string{"Label": "HIGH", "Original": "HIGH"},
            "resources":     []map[string]string{{"Id": "arn:aws:lambda:eu-west-2:123456789012:function:my-fn", "Type": "AwsLambdaFunction"}},
            "created_at":    "2024-01-15T10:00:00Z",
            "updated_at":    "2024-01-15T10:00:00Z",
        },
    },

Marshal this payload using the existing marshal helper. Post it using the existing postJSON helper.

Assert:
    if rr.Code != http.StatusCreated {
        t.Errorf("status = %d, want 201; body: %s", rr.Code, rr.Body)
    }
    if !strings.Contains(rr.Body.String(), "run_id") {
        t.Errorf("response body %q does not contain \"run_id\"", rr.Body.String())
    }

Do not alter any existing sub-test.
  </action>
  <verify>
    <automated>cd /home/hendry/ai-siem-endpoint && go test ./... -v</automated>
  </verify>
  <done>go test ./... passes. TestHandlePost shows 6 sub-tests passing (5 original + 1 new ASFF). TestPutEventAttributes passes. Total test count increases by the number of new assertions added.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| test→handlePost | Test-only; no external trust boundary — all inputs are controlled by the test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-05 | Information Disclosure | test fixtures | accept | Test data uses synthetic ARNs and account IDs that don't correspond to real AWS resources; no real credentials in test code |
| T-03-SC | Tampering | npm/pip/cargo installs | accept | No new packages in this phase |
</threat_model>

<verification>
After both tasks:
- `go test ./... -v` exits 0
- Output contains "--- PASS: TestPutEventAttributes"
- Output contains "--- PASS: TestHandlePost/asff_extended_payload_returns_201"
- `grep -c "product_arn" /home/hendry/ai-siem-endpoint/main_test.go` returns >= 2 (one in AuditRun literal, one in assertion)
- `grep -c "aws_account_id" /home/hendry/ai-siem-endpoint/main_test.go` returns >= 2
- `grep "finding-001" /home/hendry/ai-siem-endpoint/main_test.go` shows the assertion string
</verification>

<success_criteria>
1. TestPutEventAttributes asserts product_arn and aws_account_id DynamoDB attributes with correct values
2. TestPutEventAttributes asserts findings blob contains ASFF Finding fields (ID and severity label)
3. TestHandlePost has a new sub-test "asff extended payload returns 201" that passes
4. `go test ./...` exits 0 with all tests green — zero regressions
5. ASFF-05 coverage: putEvent attributes tested (unit), handler acceptance tested (integration via httptest)
</success_criteria>

<output>
Create `.planning/phases/03-asff-data-model-alignment/03-03-SUMMARY.md` when done
</output>
